#This code prints the number of charaters in a user's name
#Get the length of a string (number of character) with len()
#By passing a string to the built-in function len(), its length (number of characters) is returned as an integer value.
print(len(input("What is your name? ")))

#Notes 
#If input was "Jack"
#1st: print(len("Jack"))
#2nd: print(4)